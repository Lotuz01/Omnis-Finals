global:
  smtp_smarthost: '${SMTP_HOST}:587'
  smtp_from: '${SMTP_USER}'
  smtp_auth_username: '${SMTP_USER}'
  smtp_auth_password: '${SMTP_PASS}'
  smtp_require_tls: true
  
  # Webhook para Slack/Discord/Teams
  slack_api_url: '${SLACK_WEBHOOK_URL}'
  
  # Configurações globais
  resolve_timeout: 5m
  http_config:
    tls_config:
      insecure_skip_verify: false

route:
  group_by: ['alertname', 'cluster', 'service']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'default'
  routes:
    # Alertas críticos - notificação imediata
    - match:
        severity: critical
      receiver: 'critical-alerts'
      group_wait: 0s
      repeat_interval: 5m
      
    # Alertas de segurança - notificação imediata
    - match:
        category: security
      receiver: 'security-alerts'
      group_wait: 0s
      repeat_interval: 15m
      
    # Alertas de infraestrutura
    - match:
        category: infrastructure
      receiver: 'infrastructure-alerts'
      repeat_interval: 30m
      
    # Alertas de aplicação
    - match:
        category: application
      receiver: 'application-alerts'
      repeat_interval: 1h
      
    # Alertas de warning
    - match:
        severity: warning
      receiver: 'warning-alerts'
      repeat_interval: 2h

receivers:
  - name: 'default'
    email_configs:
      - to: '${ALERT_EMAIL_TO}'
        subject: '[Sistema Gestão] {{ .GroupLabels.alertname }}'
        body: |
          {{ range .Alerts }}
          **Alerta:** {{ .Annotations.summary }}
          **Descrição:** {{ .Annotations.description }}
          **Severidade:** {{ .Labels.severity }}
          **Instância:** {{ .Labels.instance }}
          **Horário:** {{ .StartsAt.Format "2006-01-02 15:04:05" }}
          {{ end }}
        headers:
          Subject: '[Sistema Gestão] {{ .GroupLabels.alertname }}'
          
  - name: 'critical-alerts'
    email_configs:
      - to: '${CRITICAL_ALERT_EMAIL}'
        subject: '🚨 [CRÍTICO] Sistema Gestão - {{ .GroupLabels.alertname }}'
        body: |
          🚨 **ALERTA CRÍTICO DETECTADO** 🚨
          
          {{ range .Alerts }}
          **Alerta:** {{ .Annotations.summary }}
          **Descrição:** {{ .Annotations.description }}
          **Severidade:** {{ .Labels.severity }}
          **Serviço:** {{ .Labels.job }}
          **Instância:** {{ .Labels.instance }}
          **Horário:** {{ .StartsAt.Format "2006-01-02 15:04:05" }}
          
          **Ação Requerida:** Intervenção imediata necessária!
          {{ end }}
        headers:
          Priority: 'high'
          X-Priority: '1'
          
    slack_configs:
      - api_url: '${SLACK_WEBHOOK_URL}'
        channel: '#alerts-critical'
        title: '🚨 Alerta Crítico - Sistema Gestão'
        text: |
          {{ range .Alerts }}
          *Alerta:* {{ .Annotations.summary }}
          *Descrição:* {{ .Annotations.description }}
          *Severidade:* {{ .Labels.severity }}
          *Serviço:* {{ .Labels.job }}
          *Instância:* {{ .Labels.instance }}
          *Horário:* {{ .StartsAt.Format "2006-01-02 15:04:05" }}
          {{ end }}
        color: 'danger'
        
    webhook_configs:
      - url: '${WEBHOOK_URL}'
        send_resolved: true
        http_config:
          basic_auth:
            username: '${WEBHOOK_USER}'
            password: '${WEBHOOK_PASS}'
        
  - name: 'security-alerts'
    email_configs:
      - to: '${SECURITY_ALERT_EMAIL}'
        subject: '🔒 [SEGURANÇA] Sistema Gestão - {{ .GroupLabels.alertname }}'
        body: |
          🔒 **ALERTA DE SEGURANÇA** 🔒
          
          {{ range .Alerts }}
          **Tipo de Ataque:** {{ .Labels.attack_type | default "Não especificado" }}
          **IP Origem:** {{ .Labels.source_ip | default "Desconhecido" }}
          **Alerta:** {{ .Annotations.summary }}
          **Descrição:** {{ .Annotations.description }}
          **Severidade:** {{ .Labels.severity }}
          **Horário:** {{ .StartsAt.Format "2006-01-02 15:04:05" }}
          
          **Ação Recomendada:** Verificar logs de segurança e considerar bloqueio de IP.
          {{ end }}
        headers:
          X-Priority: '2'
          
    slack_configs:
      - api_url: '${SLACK_WEBHOOK_URL}'
        channel: '#security-alerts'
        title: '🔒 Alerta de Segurança - Sistema Gestão'
        text: |
          {{ range .Alerts }}
          *Tipo:* {{ .Labels.attack_type | default "Não especificado" }}
          *IP:* {{ .Labels.source_ip | default "Desconhecido" }}
          *Descrição:* {{ .Annotations.description }}
          *Horário:* {{ .StartsAt.Format "2006-01-02 15:04:05" }}
          {{ end }}
        color: 'warning'
        
  - name: 'infrastructure-alerts'
    email_configs:
      - to: '${INFRA_ALERT_EMAIL}'
        subject: '⚙️ [INFRAESTRUTURA] Sistema Gestão - {{ .GroupLabels.alertname }}'
        body: |
          ⚙️ **ALERTA DE INFRAESTRUTURA** ⚙️
          
          {{ range .Alerts }}
          **Componente:** {{ .Labels.job }}
          **Alerta:** {{ .Annotations.summary }}
          **Descrição:** {{ .Annotations.description }}
          **Severidade:** {{ .Labels.severity }}
          **Instância:** {{ .Labels.instance }}
          **Horário:** {{ .StartsAt.Format "2006-01-02 15:04:05" }}
          {{ end }}
          
    slack_configs:
      - api_url: '${SLACK_WEBHOOK_URL}'
        channel: '#infrastructure'
        title: '⚙️ Alerta de Infraestrutura'
        color: 'warning'
        
  - name: 'application-alerts'
    email_configs:
      - to: '${DEV_ALERT_EMAIL}'
        subject: '📱 [APLICAÇÃO] Sistema Gestão - {{ .GroupLabels.alertname }}'
        body: |
          📱 **ALERTA DE APLICAÇÃO** 📱
          
          {{ range .Alerts }}
          **Aplicação:** {{ .Labels.job }}
          **Alerta:** {{ .Annotations.summary }}
          **Descrição:** {{ .Annotations.description }}
          **Severidade:** {{ .Labels.severity }}
          **Endpoint:** {{ .Labels.route | default "N/A" }}
          **Horário:** {{ .StartsAt.Format "2006-01-02 15:04:05" }}
          {{ end }}
          
    slack_configs:
      - api_url: '${SLACK_WEBHOOK_URL}'
        channel: '#development'
        title: '📱 Alerta de Aplicação'
        color: 'warning'
        
  - name: 'warning-alerts'
    email_configs:
      - to: '${WARNING_ALERT_EMAIL}'
        subject: '⚠️ [AVISO] Sistema Gestão - {{ .GroupLabels.alertname }}'
        body: |
          ⚠️ **AVISO** ⚠️
          
          {{ range .Alerts }}
          **Alerta:** {{ .Annotations.summary }}
          **Descrição:** {{ .Annotations.description }}
          **Severidade:** {{ .Labels.severity }}
          **Instância:** {{ .Labels.instance }}
          **Horário:** {{ .StartsAt.Format "2006-01-02 15:04:05" }}
          {{ end }}

inhibit_rules:
  # Inibir alertas de warning quando há críticos
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'instance']
    
  # Inibir alertas de aplicação quando a instância está down
  - source_match:
      alertname: 'InstanceDown'
    target_match_re:
      alertname: '(HighResponseTime|HighErrorRate|DatabaseConnectionFailed)'
    equal: ['instance']
    
  # Inibir alertas de banco quando o container está down
  - source_match:
      alertname: 'ContainerDown'
      container_name: 'postgres'
    target_match_re:
      alertname: '(DatabaseConnectionFailed|DatabaseHighConnections)'
    equal: ['instance']

templates:
  - '/etc/alertmanager/templates/*.tmpl'

# Configurações de teste
test_webhook:
  url: '${TEST_WEBHOOK_URL}'
  
# Configurações de clustering (para alta disponibilidade)
cluster:
  listen-address: '0.0.0.0:9094'
  peer: []
  
# Configurações de storage
storage:
  path: '/alertmanager/data'
  retention: '120h'
  
# Configurações de log
log:
  level: 'info'
  format: 'json'